<!DOCTYPE html>
<html>
  <head>
    <title>App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= javascript_include_tag "application", "data-turbo-track": "true", defer: true %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container-fluid">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav w-100">
            <li class="nav-item text-center w-100">
              <%= link_to "Agendaplus", root_path, class: "navbar-brand mx-auto" %>
            </li>
            <li class="nav-item">
              <% if user_signed_in? %>
                <%= button_to "Logout", destroy_user_session_path, class: "btn btn-light", method: :delete, data: { turbo_method: :delete }  %>
              <% else %>
                <%= link_to "Login", new_user_session_path, class: "btn btn-light" %>
              <% end %>
            </li>
          </ul>
        </div>
      </div>
    </nav>
    <% if flash[:notice].present? %>
      <div class="alert alert-success mt-3" role="alert">
        <%= notice %>
      </div>
    <% end %>
    <%= yield %>
  </body>
</html>
